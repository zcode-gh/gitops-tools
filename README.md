# gitops-tools
The tools repository to host shared code

## Repository dependencies
Please, install the following dependencies before setting up this repository:

* pre-commit
* shellcheck-py

## Repository setup
Run the follow command to set up the repository:

```
./bootstrap.sh
```
